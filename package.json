{
    "scripts": {
        "start": "concurrently \"npm run client\" \"npm run server\"",
        "client": "cd client && npm install && npm run dev",
        "server": "cd server && docker compose up --build"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}
